# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# Specify the runtime environment for your application.
# For Next.js, you'll likely need a Node.js environment.
runtime: nodejs18 # Or the Node.js version you are using

runConfig:
  maxInstances: 1
  # Add your environment variables here
  environmentVariables:
    SENDGRID_API_KEY: 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
    SENDGRID_FROM_EMAIL: vanshsharma11.2009@gmail.com

buildConfig:
  # Specify the command to build your Next.js application
  # This will typically be 'npm run build' or 'yarn build'
  build: npm run build
  # Specify the output directory of your Next.js build
  # This is typically '.next' for Next.js projects
  output: .next

# Optional: Specify the entry point of your application if needed
# entrypoint: index.js # Adjust this based on your Next.js setup
